{
  "name": "epic-character-dictionary-backend",
  "main": "index.js",
  "engines": { "node": "18" },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "firebase-admin": "^11.10.0",
    "firebase-functions": "^4.6.1"
  },
"scripts": {
  "lint": "eslint .",
  "build": "echo 'no build step defined'",
  "serve": "firebase emulators:start --only functions",
  "deploy": "firebase deploy --only functions"
}
}
